apiVersion: v1
kind: Secret
metadata:
  name: {{ include "agentshield.fullname" . }}-redis-password
  labels:
    {{- include "agentshield.labels" . | nindent 4 }}
type: Opaque
data:
  REDIS_PASSWORD: {{ include "agentshield.redisPassword" . | b64enc }}
